# Development environment variables.
# In dev, API requests go to "/api" and are proxied to the backend Gateway
# (see vite.config.ts server.proxy). Leave VITE_API_BASE_URL unset to use it.

# VITE_API_BASE_URL=/api
